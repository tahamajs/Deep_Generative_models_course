# Compatibility Notice

This repository uses `CLAUDE.md` (capitalized) as the authoritative Claude Code rule set. To avoid conflicting directives, do not place instructions here. Always read the root `CLAUDE.md` and any subdirectory `CLAUDE.md` files for context-specific rules.
