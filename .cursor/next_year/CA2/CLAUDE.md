# next_year/CA2 — Claude Code Guidance

**Purpose**: Future CA2 drafts (GANs/Flows) under `next_year/CA2`.  
**Parent Context**: Extends `../CLAUDE.md` (next_year rules) and root `../../CLAUDE.md`.

## Rules

- Draft-only area; keep separate from current-year CA2 deliverables.
- Centralize seeds/config/device at the top of notebooks or modules.
- Reports must be IEEE-formatted `.tex`; PDFs are artifacts only.
- Do not overwrite template assets; copy before modifying.

## Safety

- Avoid committing large checkpoints/datasets; keep locally unless required.
- Note any deviations from mainline CA2 patterns when prototyping.
