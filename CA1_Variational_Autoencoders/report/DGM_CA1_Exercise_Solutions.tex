\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
% The preceding line is only needed to identify funding in the first footnote. If that is unneeded, please comment it out.
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\graphicspath{{../images/}}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}

% Configure listings for Python code
\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color{green!50!black},
    stringstyle=\color{red},
    numbers=left,
    numberstyle=\tiny\color{gray},
    stepnumber=1,
    numbersep=5pt,
    backgroundcolor=\color{gray!10},
    frame=single,
    breaklines=true,
    showstringspaces=false,
    breakatwhitespace=false
}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}
\newcommand{\logvar}{\log \sigma^2}

\begin{document}

\title{Variational Autoencoders: Implementation, Analysis, and Evaluation of Probabilistic Latent Representations for Facial Image Generation\\
{\footnotesize \textsuperscript{*}Course Assignment 1, Deep Generative Models}
}

\author{\IEEEauthorblockN{1\textsuperscript{st} Mohammad Taha Majlesi}
\IEEEauthorblockA{\textit{Student ID: 8100101504}\\
\textit{Department of Computer Engineering}\\
\textit{University of Tehran}\\
Tehran, Iran}
}

\maketitle

\begin{abstract}
This report presents a comprehensive implementation and analysis of Variational Autoencoders (VAEs) for learning compressed probabilistic representations of facial image data. We implement a convolutional VAE architecture that employs hierarchical feature extraction through convolutional layers and utilizes the reparameterization trick to enable gradient-based optimization of stochastic latent variables. Through training on a dataset of 1,203 facial images with binary classification (smile/non-smile), we demonstrate effective reconstruction capabilities with 1,536:1 compression ratio, generative image synthesis through sampling from the learned latent distribution, and interpretable latent space manipulation via interpolation experiments. In a 120-epoch run, training loss decreased from 7226.17 to 837.18, while the best validation loss reached 1372.88 at epoch 60 (final validation at epoch 120: 1406.75). Analysis of the latent space reveals class-separated clusters with continuous structure, enabling semantic attribute manipulation. The model successfully generates diverse facial images from random latent samples, validating the effectiveness of the probabilistic latent space formulation.
\end{abstract}

\begin{IEEEkeywords}
Variational Autoencoders, Generative Models, Probabilistic Inference, Deep Learning, Image Generation, Latent Space, Reparameterization Trick, Evidence Lower Bound
\end{IEEEkeywords}

\section{Introduction}

Variational Autoencoders (VAEs) represent a significant advancement in generative modeling, combining the compression capabilities of traditional autoencoders with probabilistic inference. Unlike deterministic autoencoders, VAEs learn a probabilistic latent space that enables meaningful generation and interpolation of new data samples \cite{kingma2013auto}.

This project implements and evaluates a VAE for facial image data, addressing the fundamental challenge of learning meaningful latent representations that simultaneously enable accurate reconstruction, controlled generation, and semantic manipulation. We demonstrate the effectiveness of the Evidence Lower Bound (ELBO) optimization framework and the reparameterization trick in enabling end-to-end training of probabilistic generative models.

\section{Theoretical Background}

\subsection{Variational Autoencoders Framework}

Variational Autoencoders are a class of generative models that learn probabilistic representations of data by combining the compression capabilities of traditional autoencoders with variational inference principles. The key innovation of VAEs lies in introducing probabilistic latent variables, enabling both reconstruction and generation capabilities.

\paragraph{Motivation and Background:}
Traditional autoencoders learn deterministic mappings from input space to a bottleneck latent representation and back to the input space. While effective for compression and denoising, deterministic latent spaces have several limitations:
\begin{itemize}
    \item \textbf{Hole Problem}: Gaps in latent space may not correspond to valid data samples, making generation unreliable
    \item \textbf{Discontinuous Manifold}: Interpolation between latent vectors may produce unrealistic outputs
    \item \textbf{No Probabilistic Interpretation}: Cannot model uncertainty or sample from learned distribution
\end{itemize}

VAEs address these limitations by introducing probabilistic latent variables. Instead of mapping each input to a single latent vector, VAEs map inputs to probability distributions over latent space. This probabilistic formulation enables:
\begin{itemize}
    \item Meaningful sampling from the latent space
    \item Smooth interpolation between data points
    \item Uncertainty quantification in the latent representation
    \item Theoretical foundation connecting to variational inference and maximum likelihood estimation
\end{itemize}

\paragraph{Probabilistic Framework:}
The VAE models the joint distribution $p_\theta(\mathbf{x}, \mathbf{z}) = p_\theta(\mathbf{x}|\mathbf{z})p(\mathbf{z})$ where:
\begin{itemize}
    \item $p(\mathbf{z}) = \mathcal{N}(0, I)$: Prior distribution on latent variables (typically standard normal)
    \item $p_\theta(\mathbf{x}|\mathbf{z})$: Generative model (decoder) that maps latent vectors to data space
    \item $q_\phi(\mathbf{z}|\mathbf{x})$: Approximate posterior (encoder) that infers latent variables from data
\end{itemize}

The encoder $q_\phi(\mathbf{z}|\mathbf{x})$ is introduced because the true posterior $p(\mathbf{z}|\mathbf{x})$ is intractable to compute directly. The encoder approximates this posterior using a neural network that outputs the parameters (mean and variance) of a Gaussian distribution.

\subsection{Key Concepts}

\subsubsection{Probabilistic Latent Space}

Unlike deterministic autoencoders, VAEs learn a probabilistic latent space where each input $x$ is encoded into a distribution $q_\phi(z|x)$ rather than a fixed point. This enables meaningful generation and interpolation.

\subsubsection{Evidence Lower Bound (ELBO)}

The training objective maximizes the Evidence Lower Bound (ELBO), which is a lower bound on the log-likelihood of the data.

\paragraph{Derivation of ELBO:}
We wish to maximize the log-likelihood of the data:
\begin{equation}
\log p_\theta(\mathbf{x}) = \log \int p_\theta(\mathbf{x}|\mathbf{z})p(\mathbf{z}) d\mathbf{z}
\end{equation}

However, this integral is intractable. Using Jensen's inequality and introducing the approximate posterior $q_\phi(\mathbf{z}|\mathbf{x})$:

\begin{align}
\log p_\theta(\mathbf{x}) &= \log \int p_\theta(\mathbf{x}|\mathbf{z})p(\mathbf{z}) d\mathbf{z} \\
&= \log \int q_\phi(\mathbf{z}|\mathbf{x}) \frac{p_\theta(\mathbf{x}|\mathbf{z})p(\mathbf{z})}{q_\phi(\mathbf{z}|\mathbf{x})} d\mathbf{z} \\
&\geq \int q_\phi(\mathbf{z}|\mathbf{x}) \log \frac{p_\theta(\mathbf{x}|\mathbf{z})p(\mathbf{z})}{q_\phi(\mathbf{z}|\mathbf{x})} d\mathbf{z} \quad \text{(Jensen's inequality)} \\
&= \mathbb{E}_{q_\phi(\mathbf{z}|\mathbf{x})}[\log p_\theta(\mathbf{x}|\mathbf{z})] - \text{KL}(q_\phi(\mathbf{z}|\mathbf{x}) || p(\mathbf{z}))
\end{align}

The training objective is:

\begin{equation}
\mathcal{L}(\theta, \phi; \mathbf{x}) = \mathbb{E}_{q_\phi(\mathbf{z}|\mathbf{x})} [\log p_\theta(\mathbf{x}|\mathbf{z})] - \text{KL}(q_\phi(\mathbf{z}|\mathbf{x}) || p(\mathbf{z}))
\end{equation}

where:
\begin{itemize}
    \item $p_\theta(\mathbf{x}|\mathbf{z})$: Decoder (generative model) that reconstructs data from latent variables
    \item $q_\phi(\mathbf{z}|\mathbf{x})$: Encoder (recognition model) that infers latent variables from data
    \item $p(\mathbf{z})$: Prior distribution (typically $\mathcal{N}(0, I)$)
\end{itemize}

\paragraph{Interpretation of ELBO Components:}

The ELBO consists of two competing terms:

\begin{enumerate}
    \item \textbf{Reconstruction Term} $\mathbb{E}_{q_\phi(\mathbf{z}|\mathbf{x})} [\log p_\theta(\mathbf{x}|\mathbf{z})]$:
    \begin{itemize}
        \item Encourages the decoder to accurately reconstruct input data
        \item Maximized when $p_\theta(\mathbf{x}|\mathbf{z})$ assigns high probability to the original input
        \item Can be approximated by sampling $\mathbf{z} \sim q_\phi(\mathbf{z}|\mathbf{x})$ and computing $\log p_\theta(\mathbf{x}|\mathbf{z})$
        \item In practice, often uses a single sample (or multiple samples for variance reduction)
    \end{itemize}
    
    \item \textbf{Regularization Term} $-\text{KL}(q_\phi(\mathbf{z}|\mathbf{x}) || p(\mathbf{z}))$:
    \begin{itemize}
        \item Penalizes deviation of the approximate posterior from the prior
        \item Ensures the latent space has structure compatible with the prior distribution
        \item Prevents the encoder from collapsing to a point distribution (posterior collapse)
        \item Enables meaningful sampling from the prior for generation
    \end{itemize}
\end{enumerate}

\paragraph{Trade-off:}
The two terms create a fundamental trade-off:
\begin{itemize}
    \item High reconstruction weight $\rightarrow$ Better image quality, but latent space may not be smooth or generative
    \item High KL weight $\rightarrow$ Smoother latent space, but reconstructions may be blurrier
    \item Optimal balance is task-dependent and learned during training
\end{itemize}

\subsubsection{Reparameterization Trick}

The reparameterization trick is essential for enabling gradient-based optimization when the objective involves stochastic latent variables.

\paragraph{Problem with Direct Sampling:}
If we directly sample from the encoder distribution:
\begin{equation}
\mathbf{z} \sim q_\phi(\mathbf{z}|\mathbf{x}) = \mathcal{N}(\boldsymbol{\mu}_\phi(\mathbf{x}), \boldsymbol{\sigma}_\phi^2(\mathbf{x}))
\end{equation}

We cannot backpropagate gradients through the sampling operation because it involves random noise. The gradient $\nabla_\phi \mathbb{E}_{\mathbf{z} \sim q_\phi(\mathbf{z}|\mathbf{x})}[\log p_\theta(\mathbf{x}|\mathbf{z})]$ is undefined.

\paragraph{Solution: Reparameterization:}
We rewrite the sampling operation to separate the stochastic component from the learnable parameters:

\begin{equation}
\mathbf{z} = \boldsymbol{\mu}_\phi(\mathbf{x}) + \boldsymbol{\sigma}_\phi(\mathbf{x}) \odot \epsilon, \quad \epsilon \sim \mathcal{N}(0, I)
\end{equation}

where $\boldsymbol{\sigma}_\phi(\mathbf{x}) = \exp(0.5 \times \logvar_\phi(\mathbf{x}))$ to ensure positivity.

\paragraph{Why This Works:}
\begin{itemize}
    \item \textbf{Deterministic Path}: $\boldsymbol{\mu}_\phi(\mathbf{x})$ and $\boldsymbol{\sigma}_\phi(\mathbf{x})$ are deterministic functions of $\mathbf{x}$ and $\phi$, so gradients can flow through them
    \item \textbf{Stochastic Path}: $\epsilon$ is independent of $\phi$ and sampled from a fixed distribution, so it doesn't block gradients
    \item \textbf{Gradient Estimator}: The gradient can now be estimated as:
    \begin{equation}
    \nabla_\phi \mathbb{E}_{\epsilon \sim \mathcal{N}(0,I)}[\log p_\theta(\mathbf{x}|\boldsymbol{\mu}_\phi(\mathbf{x}) + \boldsymbol{\sigma}_\phi(\mathbf{x}) \odot \epsilon)] = \mathbb{E}_{\epsilon \sim \mathcal{N}(0,I)}[\nabla_\phi \log p_\theta(\mathbf{x}|\mathbf{z})]
    \end{equation}
\end{itemize}

\paragraph{Implementation Considerations:}
\begin{itemize}
    \item \textbf{Numerical Stability}: We use $\logvar$ instead of $\sigma^2$ to avoid numerical issues and ensure positivity
    \item \textbf{Clamping}: In practice, we clamp $\logvar$ to reasonable bounds (e.g., [-10, 10]) to prevent extreme values
    \item \textbf{Sampling}: For training, typically one sample per input is sufficient; for inference, can use multiple samples for better estimates
\end{itemize}

\subsubsection{Loss Function Components}

The total loss consists of two components:

\begin{equation}
\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{recon}} + \mathcal{L}_{\text{KL}}
\end{equation}

\paragraph{Reconstruction Loss:}
\begin{equation}
\mathcal{L}_{\text{recon}} = \frac{1}{N} \sum_{i=1}^{N} ||\mathbf{x}_i - \hat{\mathbf{x}}_i||^2
\end{equation}

\paragraph{KL Divergence Loss:}
The KL divergence term has a closed-form expression when both distributions are Gaussian. For $q_\phi(\mathbf{z}|\mathbf{x}) = \mathcal{N}(\boldsymbol{\mu}, \text{diag}(\boldsymbol{\sigma}^2))$ and $p(\mathbf{z}) = \mathcal{N}(0, I)$:

\begin{equation}
\text{KL}(q_\phi(\mathbf{z}|\mathbf{x}) || p(\mathbf{z})) = \sum_{j=1}^{d} \text{KL}(\mathcal{N}(\mu_j, \sigma_j^2) || \mathcal{N}(0, 1))
\end{equation}

For univariate Gaussians:
\begin{align}
\text{KL}(\mathcal{N}(\mu, \sigma^2) || \mathcal{N}(0, 1)) &= \frac{1}{2}\left(\sigma^2 + \mu^2 - 1 - \log(\sigma^2)\right) \\
&= \frac{1}{2}\left(\sigma^2 + \mu^2 - 1 - 2\log(\sigma)\right)
\end{align}

In terms of $\logvar = \log(\sigma^2)$:
\begin{equation}
\mathcal{L}_{\text{KL}} = -\frac{1}{2} \sum_{j=1}^{d} (1 + \logvar_j - \mu_j^2 - \exp(\logvar_j))
\end{equation}

\paragraph{Interpretation of KL Divergence:}
\begin{itemize}
    \item The KL term encourages the posterior $q_\phi(\mathbf{z}|\mathbf{x})$ to match the prior $p(\mathbf{z}) = \mathcal{N}(0, I)$
    \item This ensures that:
    \begin{enumerate}
        \item Latent space is centered around zero (via the $\mu_j^2$ term)
        \item Latent dimensions have unit variance on average (via the $-\exp(\logvar_j)$ term)
        \item No dimensions collapse to zero variance (via the $\logvar_j$ term)
    \end{enumerate}
    \item The regularization prevents the encoder from learning arbitrary distributions and encourages a smooth, well-behaved latent space
    \item This is crucial for generation: if we sample $\mathbf{z} \sim \mathcal{N}(0, I)$, we want the decoder to produce realistic images
\end{itemize}

\paragraph{Posterior Collapse:}
If KL divergence becomes too small during training, the posterior collapses:
\begin{itemize}
    \item Encoder outputs $\mu = 0$ and $\sigma^2 = 1$ regardless of input
    \item All inputs map to the same latent distribution
    \item Model ignores input information (mode collapse)
    \item Can be prevented by adjusting the weight of KL term or using techniques like KL annealing
\end{itemize}

\section{Methodology}

\subsection{Dataset}

The project utilizes a facial image dataset consisting of 1,203 images with binary classification:
\begin{itemize}
    \item \textbf{Smile}: 600 images containing smiles
    \item \textbf{Non-smile}: 603 images without smiles
\end{itemize}

Images are preprocessed to 128×128 resolution with RGB channels and normalized to the range $[-1, 1]$ using mean=0.5 and standard deviation=0.5, matching the Tanh activation output range. The dataset is split into 80\% training (963 samples) and 20\% validation (240 samples).

\subsection{Architecture Implementation}

Our VAE implementation employs a convolutional encoder-decoder architecture designed for 128×128 RGB images. The architecture balances reconstruction quality with generative capability through careful design of the encoder, latent space, and decoder components.

\subsubsection{Encoder Architecture}

The encoder progressively reduces spatial dimensions while increasing feature depth through four convolutional layers, followed by fully connected layers that output latent distribution parameters. The architecture is detailed in Table~\ref{tab:encoder}.

\begin{table*}[t]
\centering
\caption{Encoder Network Architecture}
\label{tab:encoder}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Layer Type} & \textbf{Configuration} & \textbf{Output Size} \\
\midrule
Conv2d & 3→32, kernel=4, stride=2, padding=1 & 64×64 \\
BatchNorm2d & - & - \\
LeakyReLU & negative\_slope=0.2 & - \\
Dropout & p=0.2 & - \\
Conv2d & 32→64, kernel=4, stride=2, padding=1 & 32×32 \\
BatchNorm2d & - & - \\
LeakyReLU & negative\_slope=0.2 & - \\
Dropout & p=0.2 & - \\
Conv2d & 64→128, kernel=4, stride=2, padding=1 & 16×16 \\
BatchNorm2d & - & - \\
LeakyReLU & negative\_slope=0.2 & - \\
Dropout & p=0.2 & - \\
Conv2d & 128→256, kernel=4, stride=2, padding=1 & 8×8 \\
LeakyReLU & negative\_slope=0.2 & - \\
Flatten & - & 16384 \\
Linear ($\mu$) & 16384→32 & 32 \\
Linear ($\log \sigma^2$) & 16384→32 & 32 \\
\bottomrule
\end{tabular}
\end{table*}

\subsubsection{Decoder Architecture}

The decoder reconstructs images from latent vectors using transposed convolutions in a symmetric structure to the encoder, as detailed in Table~\ref{tab:decoder}.

\begin{table*}[t]
\centering
\caption{Decoder Network Architecture}
\label{tab:decoder}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Layer Type} & \textbf{Configuration} & \textbf{Output Size} \\
\midrule
Linear & 32→16384 & 16384 \\
LeakyReLU & negative\_slope=0.2 & - \\
Unflatten & - & 256×8×8 \\
ConvTranspose2d & 256→128, kernel=4, stride=2, padding=1 & 16×16 \\
BatchNorm2d & - & - \\
LeakyReLU & negative\_slope=0.2 & - \\
Dropout & p=0.2 & - \\
ConvTranspose2d & 128→64, kernel=4, stride=2, padding=1 & 32×32 \\
BatchNorm2d & - & - \\
LeakyReLU & negative\_slope=0.2 & - \\
Dropout & p=0.2 & - \\
ConvTranspose2d & 64→32, kernel=4, stride=2, padding=1 & 64×64 \\
LeakyReLU & negative\_slope=0.2 & - \\
ConvTranspose2d & 32→3, kernel=4, stride=2, padding=1 & 128×128 \\
Tanh & - & 128×128 \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Training Configuration}

Training hyperparameters are selected to balance convergence speed and stability, as shown in Table~\ref{tab:hyperparams}.

\begin{table}[t]
\centering
\caption{Training Hyperparameters}
\label{tab:hyperparams}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Epochs & 120 \\
Learning Rate & 0.0005 \\
Optimizer & Adam \\
Batch Size & 64 \\
Latent Dimension & 32 \\
Hidden Dimension & 16384 \\
Train/Validation Split & 80\%/20\% \\
\bottomrule
\end{tabular}
\end{table}

\section{Implementation Details and Code Analysis}

\subsection{Exercise 1: Data Loading and Preprocessing}

\subsubsection{Objective}
Load and preprocess the facial image dataset with smile/non-smile classes.

\subsubsection{Implementation}
The data preprocessing pipeline includes:
\begin{itemize}
    \item Resizing images to 128×128 pixels
    \item Converting to tensors
    \item Normalizing pixel values to range $[-1, 1]$ using mean=0.5, std=0.5
\end{itemize}

\subsubsection{Code Analysis}
The normalization formula is: $x_{\text{norm}} = \frac{x - 0.5}{0.5} = 2(x - 0.5)$, which maps pixel values from $[0, 1]$ to $[-1, 1]$.

\paragraph{Why Normalize to $[-1, 1]$?}
\begin{itemize}
    \item \textbf{Tanh Activation}: The decoder's final layer uses Tanh activation, which outputs values in $[-1, 1]$. Normalizing inputs to the same range ensures consistency.
    \item \textbf{Gradient Flow}: Normalized inputs help with gradient flow during backpropagation, reducing the risk of vanishing/exploding gradients.
    \item \textbf{Training Stability}: Centered inputs (mean $\approx 0$) help batch normalization and other normalization layers work more effectively.
    \item \textbf{Convergence Speed}: Normalized data typically allows for faster and more stable convergence.
\end{itemize}

\paragraph{Preprocessing Pipeline Details:}
\begin{enumerate}
    \item \textbf{Resize}: Images are resized to 128×128 pixels to ensure uniform input size. This is necessary for batching and consistent processing.
    \item \textbf{ToTensor}: Converts PIL images to PyTorch tensors and scales pixel values from $[0, 255]$ to $[0, 1]$.
    \item \textbf{Normalize}: Applies the transformation $x_{\text{norm}} = \frac{x - \mu}{\sigma}$ where $\mu = 0.5$ and $\sigma = 0.5$ for each channel, mapping to $[-1, 1]$.
\end{enumerate}

\paragraph{Data Splitting:}
The 80\%/20\% train/validation split provides:
\begin{itemize}
    \item Sufficient training data (963 samples) for learning the data distribution
    \item Adequate validation data (240 samples) for monitoring generalization
    \item Prevents overfitting by regularly evaluating on unseen data
\end{itemize}

\subsubsection{Results}
\begin{itemize}
    \item Total dataset size: 1,203 images
    \item Class distribution: 600 smile, 603 non-smile
    \item Training samples: 963 (80\%)
    \item Validation samples: 240 (20\%)
\end{itemize}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{../images/output_cell_11_img_0.png}
\caption{Sample batch of preprocessed training images (4×1 grid) showing the diversity and quality of the dataset.}
\label{fig:data_samples}
\end{figure}

\subsection{Exercise 2: VAE Model Implementation}

\subsubsection{Objective}
Implement the complete VAE architecture with encoder, reparameterization trick, and decoder.

\subsubsection{Key Implementation Details}

\paragraph{Reparameterization Trick:}
The reparameterization trick is implemented as follows:
\begin{lstlisting}[language=Python]
def reparameterize(self, mean, logvar):
    logvar = torch.clamp(logvar, min=-10, max=10)
    std_dev = torch.exp(0.5 * logvar)
    epsilon = torch.randn_like(std_dev)
    return mean + epsilon * std_dev
\end{lstlisting}

\textbf{Detailed Analysis:}

\paragraph{Numerical Stability with Clamping:}
\begin{itemize}
    \item \texttt{torch.clamp(logvar, min=-10, max=10)}: 
    \begin{itemize}
        \item Prevents extreme values that could cause numerical underflow or overflow
        \item If $\logvar < -10$: $\sigma = \exp(0.5 \times (-10)) \approx 0.0067$ (very small variance)
        \item If $\logvar > 10$: $\sigma = \exp(0.5 \times 10) \approx 148.4$ (very large variance)
        \item This range is sufficient for most practical cases while preventing numerical issues
        \item Without clamping, extreme $\logvar$ values could cause $\exp()$ to overflow or produce near-zero values causing division by zero
    \end{itemize}
\end{itemize}

\paragraph{Log-Variance Parameterization:}
\begin{itemize}
    \item \texttt{std\_dev = torch.exp(0.5 * logvar)}: 
    \begin{itemize}
        \item Using $\logvar$ instead of $\sigma^2$ ensures positivity without constraints
        \item Avoids need for softplus or other positive activations
        \item Enables unconstrained optimization: the network can output any real value
        \item The factor of 0.5 comes from: $\sigma = \sqrt{\sigma^2} = \exp(\log(\sqrt{\sigma^2})) = \exp(0.5 \log(\sigma^2)) = \exp(0.5 \times \logvar)$
    \end{itemize}
\end{itemize}

\paragraph{Sampling Procedure:}
\begin{itemize}
    \item \texttt{epsilon $\sim$ $\mathcal{N}(0,I)$}: 
    \begin{itemize}
        \item Samples random noise from standard normal distribution
        \item Independent of model parameters, enabling gradient flow
        \item Same dimensionality as mean and logvar vectors (32 dimensions in our case)
        \item During training, different $\epsilon$ values are sampled each forward pass, providing stochasticity
    \end{itemize}
\end{itemize}

\paragraph{Gradient Flow:}
\begin{itemize}
    \item The reparameterization creates a deterministic path: $\mathbf{z} = \boldsymbol{\mu} + \boldsymbol{\sigma} \odot \epsilon$
    \item Gradients can flow through $\boldsymbol{\mu}$ and $\boldsymbol{\sigma}$ (deterministic functions of encoder parameters)
    \item The stochastic component $\epsilon$ doesn't block gradients because it's independent of parameters
    \item This enables end-to-end training of both encoder and decoder using standard backpropagation
\end{itemize}

\paragraph{Forward Pass:}
The forward method combines encoding, reparameterization, and decoding:

\begin{enumerate}
    \item \textbf{Encoding Phase}:
    \begin{itemize}
        \item Input image $\mathbf{x} \in \mathbb{R}^{3 \times 128 \times 128}$ passes through convolutional encoder
        \item Convolutional layers extract hierarchical features (edges → shapes → high-level features)
        \item Features are flattened and passed through fully connected layers
        \item Encoder outputs:
        \begin{itemize}
            \item $\boldsymbol{\mu}_\phi(\mathbf{x}) \in \mathbb{R}^{32}$: Mean of latent distribution
            \item $\logvar_\phi(\mathbf{x}) \in \mathbb{R}^{32}$: Log-variance of latent distribution
        \end{itemize}
    \end{itemize}
    
    \item \textbf{Reparameterization Phase}:
    \begin{itemize}
        \item Sample $\epsilon \sim \mathcal{N}(0, I)$ from standard normal distribution
        \item Compute $\boldsymbol{\sigma} = \exp(0.5 \times \logvar)$
        \item Generate latent vector: $\mathbf{z} = \boldsymbol{\mu} + \boldsymbol{\sigma} \odot \epsilon$
        \item This $\mathbf{z}$ represents a point in the latent space sampled from $q_\phi(\mathbf{z}|\mathbf{x})$
    \end{itemize}
    
    \item \textbf{Decoding Phase}:
    \begin{itemize}
        \item Latent vector $\mathbf{z}$ is expanded through fully connected layers
        \item Reshaped to 256×8×8 for transposed convolutional processing
        \item Transposed convolutions upscale feature maps: 8×8 → 16×16 → 32×32 → 64×64 → 128×128
        \item Final Tanh activation ensures output in $[-1, 1]$ range
        \item Output: $\hat{\mathbf{x}} \in \mathbb{R}^{3 \times 128 \times 128}$ (reconstructed image)
    \end{itemize}
    
    \item \textbf{Return Values}:
    \begin{itemize}
        \item \texttt{reconstructed\_output}: $\hat{\mathbf{x}}$ for computing reconstruction loss
        \item \texttt{mean}: $\boldsymbol{\mu}$ for computing KL divergence
        \item \texttt{logvar}: $\logvar$ for computing KL divergence and future reparameterization
    \end{itemize}
\end{enumerate}

\paragraph{Architecture Design Rationale:}
\begin{itemize}
    \item \textbf{Convolutional Encoder}: Captures spatial patterns and hierarchical features efficiently
    \item \textbf{Bottleneck Dimension (32)}: Forces compression while maintaining essential information
    \item \textbf{Symmetric Decoder}: Mirrors encoder structure for intuitive upsampling
    \item \textbf{BatchNorm and Dropout}: Regularization prevents overfitting and improves generalization
    \item \textbf{LeakyReLU}: Prevents dying ReLU problem, allows small negative gradients
\end{itemize}

\subsection{Exercise 3: Loss Function Implementation}

\subsubsection{Objective}
Implement the VAE loss function combining reconstruction and KL divergence terms.

\subsubsection{Implementation}
\begin{lstlisting}[language=Python]
def elbo_loss(recon, x, mu, logvar, beta=1.0):
    recon_loss = F.mse_loss(recon, x, reduction='sum') / x.size(0)
    kl = -0.5 * torch.sum(1 + logvar - mu.pow(2) - logvar.exp()) / x.size(0)
    loss = recon_loss + beta * kl
    return loss, recon_loss, kl
\end{lstlisting}

\subsubsection{Analysis}

\paragraph{Reconstruction Loss:}

The reconstruction loss measures the difference between original and reconstructed images.

\begin{itemize}
    \item \textbf{MSE with 'sum' reduction}: 
    \begin{itemize}
        \item Computes: $\sum_{i,j,k} (x_{i,j,k} - \hat{x}_{i,j,k})^2$ over all pixels and channels
        \item 'sum' rather than 'mean' gives larger loss values but keeps gradients proportional
        \item The magnitude will be averaged later when dividing by dataset size
    \end{itemize}
    
    \item \textbf{Why MSE?}:
    \begin{itemize}
        \item Assumes Gaussian noise in reconstruction: $p_\theta(\mathbf{x}|\mathbf{z}) = \mathcal{N}(\mathbf{x}; \hat{\mathbf{x}}, \sigma^2 I)$
        \item For Gaussian likelihood: $\log p_\theta(\mathbf{x}|\mathbf{z}) \propto -||\mathbf{x} - \hat{\mathbf{x}}||^2$
        \item MSE directly corresponds to maximizing log-likelihood under Gaussian assumption
        \item Works well for natural images, penalizing all pixel differences equally
    \end{itemize}
    
    \item \textbf{Alternative Loss Functions}:
    \begin{itemize}
        \item L1 loss ($|\mathbf{x} - \hat{\mathbf{x}}|$): More robust to outliers, encourages sparsity
        \item Perceptual loss: Uses features from pre-trained networks (VGG), better captures semantic similarity
        \item Adversarial loss: Combined with discriminator (VAE-GAN) for sharper images
    \end{itemize}
    
    \item \textbf{Loss Interpretation}:
    \begin{itemize}
        \item Lower loss $\rightarrow$ Better reconstruction quality
        \item Training loss decreases from $\sim$7226 (epoch 1) to $\sim$837 (epoch 120), with best validation $\sim$1373 at epoch 60
        \item This represents per-sample averaged loss across all pixels
    \end{itemize}
\end{itemize}

\paragraph{KL Divergence Loss:}

The KL divergence term implements the closed-form expression for Gaussian distributions.

\begin{itemize}
    \item \textbf{Mathematical Formulation}:
    \begin{itemize}
        \item For diagonal Gaussian: $q_\phi(\mathbf{z}|\mathbf{x}) = \prod_{j=1}^{d} \mathcal{N}(z_j; \mu_j, \sigma_j^2)$
        \item KL divergence decomposes: $\text{KL}(q||p) = \sum_{j=1}^{d} \text{KL}(q_j||p_j)$
        \item Each term: $\text{KL}(\mathcal{N}(\mu_j, \sigma_j^2) || \mathcal{N}(0, 1)) = \frac{1}{2}(\sigma_j^2 + \mu_j^2 - 1 - \log(\sigma_j^2))$
        \item Implementation uses: $-\frac{1}{2}\sum(1 + \logvar - \mu^2 - \exp(\logvar))$
    \end{itemize}
    
    \item \textbf{Regularization Mechanism}:
    \begin{itemize}
        \item $\mu_j^2$ term: Encourages means to be close to zero
        \item $\sigma_j^2$ term: Encourages variances to be close to 1
        \item $-\log(\sigma_j^2)$ term: Prevents variance collapse (penalizes very small variances)
        \item Together: Shapes latent distribution to match $\mathcal{N}(0, I)$
    \end{itemize}
    
    \item \textbf{Training Dynamics}:
    \begin{itemize}
        \item Initially: KL loss may be high as encoder learns to match prior
        \item During training: Decreases as posterior converges toward prior
        \item At convergence: Should stabilize, indicating balanced regularization
        \item Too low: Risk of posterior collapse (encoder ignores input)
        \item Too high: Over-regularization (blurry reconstructions)
    \end{itemize}
    
    \item \textbf{Implications for Latent Space}:
    \begin{itemize}
        \item Smooth manifold: Regularization ensures continuous, interpolable latent space
        \item Meaningful generation: Sampling $\mathbf{z} \sim \mathcal{N}(0, I)$ produces realistic images
        \item Class separation: While regularized, latent space can still learn discriminative features
        \item Dimensionality: Optimal latent dimension balances capacity vs. regularization
    \end{itemize}
\end{itemize}

\paragraph{Balance Between Terms:}
\begin{itemize}
    \item High reconstruction loss $\rightarrow$ Poor image quality
    \item High KL loss $\rightarrow$ Over-regularization, blurry reconstructions
    \item Optimal balance is achieved through training
\end{itemize}

\subsection{Exercise 4: Training Loop}

\subsubsection{Objective}
Train the VAE model with proper tracking of training and validation losses.

\subsubsection{Training Process}
For each epoch:
\begin{enumerate}
    \item \textbf{Training Phase:}
    \begin{itemize}
        \item Set model to training mode
        \item For each batch: forward pass, compute loss, backpropagate, update parameters
        \item Accumulate losses
    \end{itemize}
    \item \textbf{Validation Phase:}
    \begin{itemize}
        \item Set model to evaluation mode
        \item Disable gradient computation
        \item Compute validation loss without parameter updates
    \end{itemize}
\end{enumerate}

\subsubsection{Results Analysis}

\paragraph{Training Convergence Analysis:}

The training showed strong early improvement, followed by late-epoch overfitting:

\begin{itemize}
    \item \textbf{Initial Stage (Epoch 1)}:
    \begin{itemize}
        \item Training Loss: 7226.17, Validation Loss: 5639.53
        \item High initial loss is expected for random initialization
        \item The model quickly exits this regime in early epochs
    \end{itemize}
    
    \item \textbf{Rapid Learning Phase (Epochs 1-10)}:
    \begin{itemize}
        \item Training: 7226.17 → 1987.00 (72.5\% reduction)
        \item Validation: 5639.53 → 1914.21 (66.1\% reduction)
        \item Model quickly learns core facial structure and low-frequency details
        \item Both reconstruction and KL terms improve substantially
    \end{itemize}
    
    \item \textbf{Refinement Phase (Epochs 10-30)}:
    \begin{itemize}
        \item Training: 1987.00 → 1285.54 (35.3\% additional reduction)
        \item Validation: 1914.21 → 1470.59 (23.2\% additional reduction)
        \item Improvements continue but with a slower slope
        \item Curves begin to flatten relative to the initial phase
    \end{itemize}
    
    \item \textbf{Best Validation Phase (around Epoch 60)}:
    \begin{itemize}
        \item Training at epoch 60: 1049.79
        \item Best validation: 1372.88 (epoch 60)
        \item This checkpoint provides the strongest held-out performance in this run
    \end{itemize}
    
    \item \textbf{Late Training Phase (Epochs 61-120)}:
    \begin{itemize}
        \item Final training loss: 837.18
        \item Final validation loss: 1406.75
        \item Continued optimization reduces training loss but does not improve validation beyond epoch 60
    \end{itemize}
\end{itemize}

\paragraph{Loss Component Analysis:}

From the training loss curves (Figure 4):

\begin{itemize}
    \item \textbf{Reconstruction Loss Trend}:
    \begin{itemize}
        \item Starts high (dominant term initially)
        \item Decreases rapidly in early epochs
        \item Stabilizes as model learns to reconstruct main features
        \item Remains the larger component (as expected)
    \end{itemize}
    
    \item \textbf{KL Divergence Trend}:
    \begin{itemize}
        \item Starts relatively low (prior regularization)
        \item Increases initially as encoder learns non-trivial mappings
        \item Then decreases as posterior aligns with prior
        \item Stabilizes at moderate level maintaining regularization
    \end{itemize}
    
    \item \textbf{Balance}:
    \begin{itemize}
        \item Both losses decrease together in the early and mid training regime
        \item No indication of posterior collapse (KL remains non-trivial throughout training)
        \item Late epochs show train/val divergence, indicating overfitting after the best-validation point
    \end{itemize}
\end{itemize}

\paragraph{Generalization Assessment:}

\begin{itemize}
    \item \textbf{Train-Validation Gap}:
    \begin{itemize}
        \item Final gap: 569.57 (validation higher than training; 68.0\% relative to training)
        \item This indicates noticeable overfitting in late epochs
        \item The best generalization checkpoint occurs before the end of training
    \end{itemize}
    
    \item \textbf{Validation Performance}:
    \begin{itemize}
        \item Validation improves strongly in early/mid training and reaches its minimum at epoch 60
        \item After epoch 60, validation fluctuates upward while training continues to decrease
        \item This train/val divergence motivates checkpoint-based model selection
    \end{itemize}
    
    \item \textbf{Early Stopping Perspective}:
    \begin{itemize}
        \item A practical stopping region is near epoch 60 for this configuration
        \item Extending to epoch 120 is still useful for full curve analysis in the report
        \item The best-validation checkpoint (`best.pt`) is retained for deployment/evaluation
    \end{itemize}
\end{itemize}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{../images/output_cell_21_img_0.png}
\caption{Training loss curves showing reconstruction loss and KL divergence over 120 epochs. The run converges rapidly in early epochs and reaches best validation around epoch 60.}
\label{fig:training_loss}
\end{figure}

\section{Experimental Results}

\subsection{Image Reconstruction}

\subsubsection{Objective}
Evaluate the VAE's ability to compress and reconstruct input images.

\subsubsection{Qualitative Analysis}

The VAE demonstrates effective reconstruction capabilities with insightful characteristics:

\paragraph{Feature Preservation:}
\begin{itemize}
    \item \textbf{Facial Structure}: Overall face shape and proportions are accurately maintained
    \item \textbf{Key Features}: Eyes, nose, and mouth positions are preserved with high fidelity
    \item \textbf{Facial Landmarks}: Important landmarks (eye corners, nose tip, mouth corners) align well
    \item \textbf{Orientation}: Face angles and poses are correctly reconstructed
    \item \textbf{Indicates}: Encoder successfully captures essential structural information in 32-dimensional latent space
\end{itemize}

\paragraph{Expression Retention:}
\begin{itemize}
    \item \textbf{Emotional Expressions}: Smile/non-smile expressions are maintained in reconstructions
    \item \textbf{Facial Muscle Patterns}: Expression-related facial muscle patterns are preserved
    \item \textbf{Key Characteristics}: Distinctive facial characteristics of individuals are retained
    \item \textbf{Indicates}: Latent space encodes both identity and expression information
\end{itemize}

\paragraph{Slight Smoothing:}
\begin{itemize}
    \item \textbf{High-Frequency Details}: Fine texture details and high-frequency patterns may be slightly blurred
    \item \textbf{Edge Sharpness}: Some edges are softened compared to originals
    \item \textbf{Causes}:
    \begin{enumerate}
        \item \textbf{Information Bottleneck}: Compression from 49,152 pixels (128×128×3) to 32 dimensions loses fine details
        \item \textbf{KL Regularization}: Encourages smooth latent distributions, penalizing overly sharp reconstructions
        \item \textbf{Probabilistic Nature}: Stochastic encoding introduces averaging effect
    \end{enumerate}
    \item \textbf{Trade-off}: Acceptable smoothing in exchange for generative capability and smooth latent manifold
\end{itemize}

\paragraph{Consistency:}
\begin{itemize}
    \item \textbf{Cross-Sample Quality}: Reconstruction quality remains consistent across diverse samples
    \item \textbf{Lighting Variations}: Handles different lighting conditions effectively
    \item \textbf{Demographic Diversity}: Works well across different demographics and appearances
    \item \textbf{Indicates}: Model learns generalizable facial representations, not overfitted to specific samples
\end{itemize}

\paragraph{Compression Analysis:}
The model achieves impressive compression:
\begin{itemize}
    \item \textbf{Compression Ratio}: 49,152 dimensions → 32 dimensions = 1,536:1 compression
    \item \textbf{Information Retained}: Despite compression, essential visual information is preserved
    \item \textbf{Efficiency}: Demonstrates that facial images lie on a low-dimensional manifold
    \item \textbf{Implication}: Most pixel variations are redundant; VAE learns to extract essential features
\end{itemize}

\subsubsection{Visual Results}

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\textwidth]{../images/output_cell_19_img_0.png}
\caption{Image reconstruction results (Batch 1). Top row: original images. Bottom row: reconstructed images.}
\label{fig:recon1}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\textwidth]{../images/output_cell_19_img_1.png}
\caption{Additional reconstruction examples (Batch 2) showing consistency in quality across different samples.}
\label{fig:recon2}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\textwidth]{../images/output_cell_19_img_2.png}
\caption{Further reconstruction results (Batch 3) demonstrating the model's ability to handle diverse facial expressions and appearances.}
\label{fig:recon3}
\end{figure*}

\subsubsection{Quantitative Metrics}
\begin{itemize}
    \item Mean training reconstruction-related loss decreases from $\sim$7226 (epoch 1) to $\sim$837 (epoch 120), while best validation is $\sim$1373 at epoch 60
    \item Visual inspection confirms high perceptual similarity between originals and reconstructions
    \item Structural details maintained while high-frequency noise is suppressed
\end{itemize}

\subsection{Image Generation}

\subsubsection{Objective}
Generate new images by sampling from the learned latent distribution.

\subsubsection{Method}

Image generation from learned distribution involves:

\begin{enumerate}
    \item \textbf{Prior Sampling}: Sample random latent vectors from standard normal prior:
    \begin{equation}
    \mathbf{z} \sim \mathcal{N}(0, I), \quad \mathbf{z} \in \mathbb{R}^{32}
    \end{equation}
    \begin{itemize}
        \item Each component $z_i \sim \mathcal{N}(0, 1)$ independently
        \item Different samples explore different regions of latent space
    \end{itemize}
    
    \item \textbf{Decoding}: Pass sampled vectors through trained decoder:
    \begin{equation}
    \hat{\mathbf{x}} = \text{Decoder}_\theta(\mathbf{z})
    \end{equation}
    \begin{itemize}
        \item Decoder learned mapping from latent space to image space
        \item Since latent space matches prior, samples should produce realistic images
    \end{itemize}
    
    \item \textbf{Visualization}: Display generated samples in a grid format
    \begin{itemize}
        \item Allows assessment of diversity and quality
        \item Helps identify mode collapse or other generation issues
    \end{itemize}
\end{enumerate}

\paragraph{Why This Works:}
\begin{itemize}
    \item \textbf{Trained Alignment}: During training, KL divergence forces $q_\phi(\mathbf{z}|\mathbf{x})$ toward $\mathcal{N}(0, I)$
    \item \textbf{Decoder Training}: Decoder sees samples from $q_\phi(\mathbf{z}|\mathbf{x})$ which approximately matches prior
    \item \textbf{Generalization}: Decoder learns to map any point in latent space near prior to realistic images
    \item \textbf{Smooth Manifold}: Regularized latent space ensures decoder is trained on smooth, continuous region
\end{itemize}

\subsubsection{Results Analysis}

\textbf{Characteristics of Generated Images:}
\begin{itemize}
    \item \textbf{Diversity}: Generated samples exhibit variation in facial features, expressions, and appearances
    \item \textbf{Realism}: Images maintain realistic facial proportions and structure
    \item \textbf{Coherence}: Generated faces are coherent without major artifacts or distortions
    \item \textbf{Distribution Coverage}: Samples span the learned data manifold effectively
\end{itemize}

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\textwidth]{../images/output_cell_23_img_0.png}
\caption{Generated images from random latent vectors sampled from $\mathcal{N}(0, I)$. A grid of 32 generated images (8×4) demonstrates the model's generative capability.}
\label{fig:generation}
\end{figure*}

\subsubsection{Analysis}

\paragraph{Generation Quality Assessment:}
\begin{itemize}
    \item \textbf{Realistic Appearance}: Generated images exhibit realistic facial features and proportions
    \item \textbf{Structural Coherence}: Faces are well-formed without major distortions or artifacts
    \item \textbf{Successful Mapping}: Model successfully maps from standard normal prior $\mathcal{N}(0, I)$ to realistic facial images
    \item \textbf{Indicates}: Decoder learned meaningful mapping from latent space to image space
\end{itemize}

\paragraph{Distribution Coverage:}
\begin{itemize}
    \item \textbf{Diversity}: Generated samples show variation in:
    \begin{itemize}
        \item Facial features (eyes, nose, mouth shapes)
        \item Expressions (various smile intensities)
        \item Appearances (different demographics, ages)
        \item Orientations (face angles)
    \end{itemize}
    \item \textbf{No Mode Collapse}: 
    \begin{itemize}
        \item Different latent samples produce different images
        \item No repetitive or identical generations
        \item Indicates proper exploration of data manifold
    \end{itemize}
    \item \textbf{Indicates}: Latent space captures meaningful data variation, not just memorization
\end{itemize}

\paragraph{Latent Space Validation:}
\begin{itemize}
    \item \textbf{Prior Matching}: Successful generation validates that $q_\phi(\mathbf{z}|\mathbf{x}) \approx \mathcal{N}(0, I)$
    \item \textbf{Decoder Generalization}: Decoder works well on samples from prior, not just training posteriors
    \item \textbf{Manifold Learning}: Model learned the underlying data manifold structure
    \item \textbf{Generative Capability}: Confirms VAE's ability to generate new, unseen samples
\end{itemize}

\paragraph{Limitations Observed:}
\begin{itemize}
    \item \textbf{Minor Artifacts}: Some generated images may have slight artifacts or blurriness
    \item \textbf{Quality Variation}: Generated quality may vary between samples
    \item \textbf{Resolution}: Limited to 128×128 resolution, fine details may be lost
    \item \textbf{Expected}: These are normal for VAE-based generation; GANs typically produce sharper but less diverse images
\end{itemize}

\subsection{Latent Space Interpolation}

\subsubsection{Objective}
Explore latent space structure through interpolation between class means.

\subsubsection{Method}
\begin{enumerate}
    \item Compute mean latent vectors for "smile" and "non-smile" classes:
    \begin{align}
    \mu_{\text{smile}} &= \frac{1}{|\mathcal{S}|} \sum_{\mathbf{x} \in \mathcal{S}} \mathbb{E}_{q_\phi(\mathbf{z}|\mathbf{x})}[\mathbf{z}] \\
    \mu_{\text{non-smile}} &= \frac{1}{|\mathcal{N}|} \sum_{\mathbf{x} \in \mathcal{N}} \mathbb{E}_{q_\phi(\mathbf{z}|\mathbf{x})}[\mathbf{z}]
    \end{align}
    \item Define direction vector: $\delta = \mu_{\text{smile}} - \mu_{\text{non-smile}}$
    \item Perform interpolation with $\alpha \in [-3, 3]$:
    \begin{equation}
    \mathbf{z}_{\text{interpolated}} = \mathbf{z}_{\text{sample}} + \alpha \times \delta
    \end{equation}
    \item Decode interpolated vectors to generate images
\end{enumerate}

\subsubsection{Results Analysis}

\textbf{Observations:}
\begin{itemize}
    \item \textbf{Smooth Transition}: Gradual morphological changes observed across interpolation steps
    \item \textbf{Attribute Control}: Smile intensity changes progressively while facial identity is preserved
    \item \textbf{Feature Disentanglement}: The interpolation demonstrates that smile attribute is encoded in a semantically meaningful direction
    \item \textbf{Continuous Manifold}: Smooth transitions validate the latent space forms a continuous, well-structured manifold
\end{itemize}

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\textwidth]{../images/output_cell_25_img_0.png}
\caption{Latent space interpolation along the direction $\delta = \mu_{\text{smile}} - \mu_{\text{non-smile}}$ with $\alpha$ values from -3 to 3. The sequence (left to right) shows gradual transition from non-smile to smile expression while preserving facial identity.}
\label{fig:interpolation}
\end{figure*}

\subsubsection{Interpretation}

\paragraph{Interpolation Dynamics:}
\begin{itemize}
    \item \textbf{Parameter $\alpha$ Control}:
    \begin{itemize}
        \item $\alpha = -3$: Strong suppression of smile, more neutral/negative expression
        \item $\alpha = 0$: Base expression (original sample's expression)
        \item $\alpha = +3$: Strong enhancement of smile, more positive expression
        \item Smooth transition indicates continuous latent manifold
    \end{itemize}
    
    \item \textbf{Facial Identity Preservation}:
    \begin{itemize}
        \item Core facial structure (shape, features) remains consistent across interpolation
        \item Only expression changes; identity information is preserved
        \item Indicates successful attribute disentanglement
    \end{itemize}
    
    \item \textbf{Semantic Encoding}:
    \begin{itemize}
        \item Direction $\delta = \mu_{\text{smile}} - \mu_{\text{non-smile}}$ encodes semantic meaning
        \item This direction in latent space corresponds to smile attribute
        \item Other attributes may be encoded in different directions
    \end{itemize}
\end{itemize}

\paragraph{Disentanglement Analysis:}
\begin{itemize}
    \item \textbf{Partial Disentanglement}: 
    \begin{itemize}
        \item Expression (smile) is disentangled from identity
        \item Can manipulate expression independently
        \item However, perfect disentanglement is challenging without explicit constraints
    \end{itemize}
    
    \item \textbf{Factors Affecting Disentanglement}:
    \begin{itemize}
        \item KL regularization encourages compact representations but doesn't enforce disentanglement
        \item Without explicit constraints, features may be correlated
        \item Advanced methods like $\beta$-VAE or FactorVAE improve disentanglement
    \end{itemize}
    
    \item \textbf{Continuous Manifold}:
    \begin{itemize}
        \item Smooth transitions validate well-structured latent space
        \item No gaps or discontinuities in interpolation path
        \item Enables controlled attribute manipulation
    \end{itemize}
\end{itemize}

\paragraph{Generalization to Other Attributes:}
This interpolation method can be extended to other attributes:
\begin{itemize}
    \item Age: Interpolate between young and old class means
    \item Gender: Interpolate between male and female class means
    \item Pose: Interpolate between different face orientations
    \item Lighting: Interpolate between different lighting conditions (if classes available)
\end{itemize}

\subsection{Latent Space Visualization}

\subsubsection{Objective}
Visualize the structure of the learned latent space using dimensionality reduction.

\subsubsection{Method}
\begin{enumerate}
    \item Extract latent vectors from 500 training samples using the encoder
    \item Apply t-SNE to reduce from 32 dimensions to 2D
    \item Color points by class labels (smile/non-smile)
\end{enumerate}

\subsubsection{Results Analysis}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{../images/output_cell_27_img_0.png}
\caption{2D visualization of latent space using t-SNE (500 samples). Points are colored by class label. Good separation between smile and non-smile classes with continuous structure.}
\label{fig:tsne}
\end{figure}

\textbf{t-SNE Visualization Characteristics:}
\begin{itemize}
    \item \textbf{Class Separation}: Distinct clusters visible for smile and non-smile classes
    \item \textbf{Cluster Coherence}: Points within each class cluster tightly together
    \item \textbf{Continuous Manifold}: Smooth transitions between clusters indicate continuous latent space structure
    \item \textbf{Good Separability}: Suggests encoder learns discriminative features
\end{itemize}

\subsubsection{Interpretation}

\paragraph{t-SNE Visualization Analysis:}
The t-SNE projection reveals important properties of the learned latent space:

\begin{itemize}
    \item \textbf{Class Separation}:
    \begin{itemize}
        \item Distinct clusters for smile and non-smile classes visible
        \item Minimal overlap indicates good class discriminability
        \item Suggests encoder learns features that distinguish between classes
        \item Note: t-SNE preserves local structure but may distort global structure
    \end{itemize}
    
    \item \textbf{Cluster Coherence}:
    \begin{itemize}
        \item Points within each class cluster tightly together
        \item Indicates similar samples map to similar regions in latent space
        \item Validates consistent encoding of similar facial expressions
    \end{itemize}
    
    \item \textbf{Continuous Structure}:
    \begin{itemize}
        \item Smooth transitions between clusters visible
        \item No large gaps or disconnected regions
        \item Validates KL regularization maintains smooth, continuous manifold
    \end{itemize}
    
    \item \textbf{Dimensionality Reduction Artifacts}:
    \begin{itemize}
        \item t-SNE reduces 32D to 2D, some information is lost
        \item Actual latent space may have different structure
        \item Visual separation suggests but doesn't guarantee good separation in 32D
    \end{itemize}
\end{itemize}

\paragraph{Implications:}
\begin{itemize}
    \item \textbf{Discriminative Features}: VAE learns class-discriminative representations despite unsupervised training
    \item \textbf{Regularization Effectiveness}: KL regularization maintains smoothness while allowing class separation
    \item \textbf{Generative Quality}: Well-separated but continuous structure enables both generation and discrimination
    \item \textbf{Latent Organization}: Latent space organizes data in semantically meaningful way
\end{itemize}

\paragraph{Limitations of Visualization:}
\begin{itemize}
    \item \textbf{Projection Effects}: t-SNE may distort distances and neighborhood relationships
    \item \textbf{2D Limitation}: 32-dimensional structure cannot be fully captured in 2D
    \item \textbf{Interpretation Caution}: Visual separation doesn't guarantee perfect separation in high dimensions
    \item \textbf{Alternative Methods}: PCA or UMAP could provide complementary views
\end{itemize}

\subsection{Latent Dimension Analysis}

\subsubsection{Objective}
Analyze the utilization of different latent dimensions.

\subsubsection{Method}
\begin{enumerate}
    \item For each sample, extract standard deviation: $\sigma_j = \exp(0.5 \times \logvar_j)$
    \item Compute mean standard deviation per dimension across dataset
    \item Visualize with bar chart
\end{enumerate}

\subsubsection{Results Analysis}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{../images/output_cell_29_img_0.png}
\caption{Mean standard deviation per latent dimension (32 dimensions) across 500 dataset samples. Dimensions with higher standard deviation are more actively used.}
\label{fig:dim_analysis}
\end{figure}

\textbf{Key Findings:}
\begin{itemize}
    \item \textbf{Balanced Utilization}: Most dimensions show varying levels of activity
    \item \textbf{Active Dimensions}: Higher standard deviation dimensions encode salient, variable features
    \item \textbf{Reserve Dimensions}: Lower standard deviation dimensions provide capacity for diversity and interpolation
    \item \textbf{No Collapsed Dimensions}: All dimensions show non-zero activity, indicating effective latent space usage
\end{itemize}

\subsubsection{Interpretation}

\paragraph{Dimension Utilization Patterns:}
The distribution of standard deviations across latent dimensions reveals how the model uses its capacity:

\begin{itemize}
    \item \textbf{Active Dimensions} (High $\sigma$):
    \begin{itemize}
        \item Dimensions with higher standard deviation encode more variable features
        \item Likely encode:
        \begin{itemize}
            \item Facial expression variations (smile intensity, emotions)
            \item Pose and orientation variations
            \item Lighting and appearance variations
            \item Demographic differences (age, gender, ethnicity)
        \end{itemize}
        \item These dimensions contribute significantly to diversity and variation
    \end{itemize}
    
    \item \textbf{Reserve Dimensions} (Low $\sigma$):
    \begin{itemize}
        \item Dimensions with lower standard deviation but non-zero activity
        \item Likely encode:
        \begin{itemize}
            \item Common structural features (face shape, basic layout)
            \item Invariant features shared across samples
            \item Identity-preserving characteristics
        \end{itemize}
        \item Provide capacity for smooth interpolation and fine-grained control
    \end{itemize}
    
    \item \textbf{No Collapsed Dimensions}:
    \begin{itemize}
        \item All dimensions show non-zero activity
        \item No dimensions completely unused ($\sigma \approx 0$)
        \item Indicates efficient use of latent space capacity
    \end{itemize}
\end{itemize}

\paragraph{Feature Learning Analysis:}
\begin{itemize}
    \item \textbf{Discriminative Features}: 
    \begin{itemize}
        \item High-variance dimensions likely encode class-discriminative information
        \item Help distinguish between smile and non-smile expressions
    \end{itemize}
    
    \item \textbf{Generative Features}:
    \begin{itemize}
        \item All dimensions contribute to generation capability
        \item Combined effort creates diverse, realistic samples
    \end{itemize}
    
    \item \textbf{Balanced Representation}:
    \begin{itemize}
        \item Mix of active and reserve dimensions
        \item Provides both variation and stability
        \item Enables both generation and reconstruction tasks
    \end{itemize}
\end{itemize}

\paragraph{Optimal Dimension Analysis:}
\begin{itemize}
    \item \textbf{Current Setup}: 32 dimensions
    \begin{itemize}
        \item Provides sufficient capacity for facial image manifold
        \item Not too large (avoids overfitting and poor regularization)
        \item Not too small (maintains reconstruction quality)
    \end{itemize}
    
    \item \textbf{Trade-offs}:
    \begin{itemize}
        \item More dimensions: Better reconstruction, but harder to regularize
        \item Fewer dimensions: Easier to regularize, but may lose important details
        \item 32 dimensions appears to balance these concerns well
    \end{itemize}
    
    \item \textbf{Evidence}:
    \begin{itemize}
        \item Good reconstruction quality with 32 dimensions
        \item Successful generation from prior distribution
        \item Capacity is sufficient for representation learning, while generalization still depends on training duration and checkpoint selection
    \end{itemize}
\end{itemize}

\paragraph{Practical Applications:}
Understanding dimension utilization can help with:
\begin{itemize}
    \item \textbf{Latent Dimension Selection}: Choose appropriate dimensionality for new tasks
    \item \textbf{Regularization}: Adjust KL weight based on utilization patterns
    \item \textbf{Interpretability}: Investigate what high-variance dimensions encode
    \item \textbf{Compression}: Potentially reduce dimensions if many are underutilized
\end{itemize}

\section{Discussion}

\subsection{Reconstruction Quality Analysis}

The VAE reconstructs input images with strong structural fidelity while compressing from 49,152 dimensions (128×128×3) to 32 latent variables (1,536:1). Across the three reconstruction grids (Figures~\ref{fig:recon1}--\ref{fig:recon3}), coarse identity cues and expression-related geometry are preserved. The dominant residual errors are in high-frequency textures (skin details, hair boundaries, subtle illumination transitions), which is expected for MSE-optimized VAEs under strong compression.

From a training dynamics perspective, reconstruction quality improves sharply in the first 10 epochs and continues to improve more gradually afterward. The final training objective is substantially lower than the initial objective, confirming successful optimization of both reconstruction and KL terms. The model therefore learns a useful low-dimensional manifold, but with the known smoothness-vs-sharpness trade-off of standard VAEs.

\subsection{Generative Performance Evaluation}

Experimental results demonstrate effective generative capability: random samples from $z \sim \mathcal{N}(0, I)$ yield coherent facial images with visible diversity in pose, expression intensity, and appearance attributes (Figure~\ref{fig:generation}). While some samples remain slightly blurry, global facial topology is stable and realistic.

Importantly, generated examples do not collapse to a small set of repeated templates. Diversity in generated grids, together with usable interpolation behavior, indicates that the decoder learned a broad region of the latent manifold rather than a narrow memorized subset.

\subsection{Latent Space Structure}

The latent space exhibits semantically meaningful structure along three complementary views:
\begin{itemize}
    \item \textbf{Interpolation semantics}: The direction $\delta = \mu_{\text{smile}} - \mu_{\text{non-smile}}$ yields smooth and controllable expression transitions (Figure~\ref{fig:interpolation}), indicating usable attribute directionality.
    \item \textbf{Class organization}: The t-SNE projection (Figure~\ref{fig:tsne}) shows partial smile/non-smile separation with continuity between groups, consistent with shared facial structure and expression overlap.
    \item \textbf{Dimension utilization}: The latent-std profile (Figure~\ref{fig:dim_analysis}) indicates non-trivial activity across dimensions rather than complete collapse to a tiny subset.
\end{itemize}

Together, these observations support the claim that the model learned a structured and navigable representation, even though fully disentangled factors are not guaranteed in this baseline setup.

\subsection{Quantitative Run Summary (120 Epochs)}

Table~\ref{tab:runsummary} consolidates key checkpoints from the canonical script run used to regenerate all report figures.

\begin{table}[t]
\centering
\caption{Key Metrics from Canonical 120-Epoch Run}
\label{tab:runsummary}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Checkpoint} & \textbf{Train Loss} & \textbf{Val Loss} \\
\midrule
Epoch 1 & 7226.17 & 5639.53 \\
Epoch 10 & 1987.00 & 1914.21 \\
Epoch 30 & 1285.54 & 1470.59 \\
Epoch 60 (best val) & 1049.79 & 1372.88 \\
Epoch 120 (final) & 837.18 & 1406.75 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Model Selection and Generalization Strategy}

The run exhibits a common deep-learning pattern: continuous reduction in training loss with validation improvements peaking earlier (epoch 60). This implies:
\begin{itemize}
    \item For \textbf{best held-out quality}, use the best-validation checkpoint (`best.pt`, epoch 60).
    \item For \textbf{full trajectory analysis}, retain the complete 120-epoch curve and final checkpoint (`last.pt`, epoch 120).
\end{itemize}

This separation between optimization progress and generalization peak is important for rigorous reporting and practical deployment.

\subsection{Reproducibility and Artifact Traceability}

The full pipeline is script-first and reproducible from the project root:
\begin{lstlisting}[language=bash]
source /Users/tahamajs/Documents/uni/venv/bin/activate
python code/vae_training.py \
  --data-root train \
  --out-dir output \
  --images-dir images \
  --checkpoint-dir output/checkpoints \
  --epochs 120 \
  --batch-size 64 \
  --lr 5e-4 \
  --beta 1.0 \
  --device auto \
  --num-workers 0 \
  --analysis-samples 500 \
  --save-report-plots
\end{lstlisting}

Primary reproducibility artifacts generated by this run:
\begin{itemize}
    \item \texttt{output/train\_log.csv}: per-epoch train/validation ELBO, reconstruction, and KL.
    \item \texttt{output/run\_summary.json}: normalized metrics summary used for report synchronization.
    \item \texttt{output/checkpoints/best.pt} and \texttt{output/checkpoints/last.pt}: model selection and final-state checkpoints.
    \item \texttt{images/output\_cell\_*.png}: report-bound figures referenced directly in this document.
\end{itemize}

\subsection{Limitations and Future Work}

Current limitations include:
\begin{itemize}
    \item \textbf{Resolution ceiling}: 128×128 inputs limit fine-grained detail fidelity.
    \item \textbf{Late-epoch overfitting}: validation performance degrades after the best checkpoint region.
    \item \textbf{Limited disentanglement control}: baseline ELBO does not explicitly enforce factor disentanglement.
    \item \textbf{Compute constraints}: CPU-only training significantly increases iteration time and limits hyperparameter exploration.
\end{itemize}

High-value future extensions include:
\begin{itemize}
    \item $\beta$-VAE or KL annealing for improved latent factor structure \cite{higgs2017beta}.
    \item Higher-capacity decoders or perceptual losses to reduce blur.
    \item Conditional VAE variants for explicit attribute control.
    \item Stronger model-selection protocol (early stopping on validation trend, multi-seed confidence intervals).
\end{itemize}

\section{Conclusion}

This work delivers a complete, reproducible VAE pipeline for facial reconstruction and generation, together with synchronized report artifacts and metrics. The model achieves strong compression (1,536:1), stable generation from the prior, and interpretable latent manipulations. The 120-epoch experimental trace shows substantial optimization progress, with best held-out performance at epoch 60 and expected late-epoch overfitting behavior thereafter.

From an engineering perspective, the project now includes end-to-end reproducibility (script-first execution, checkpointing, figure regeneration, and report synchronization). From a modeling perspective, results validate the utility of variational latent representations for unsupervised facial representation learning, while also highlighting practical opportunities for improved generalization and sharper reconstruction quality in future iterations.

\section{Appendix: Figure Provenance}
\begin{itemize}
    \item \textbf{Figure: output\_cell\_11\_img\_0.png} — Exercise 1 reconstruction sample grid.
    \item \textbf{Figures: output\_cell\_19\_img\_{0,1,2}.png} — Exercise 2 reconstruction grids across batches.
    \item \textbf{Figure: output\_cell\_21\_img\_0.png} — Training/validation ELBO and loss curves.
    \item \textbf{Figure: output\_cell\_23\_img\_0.png} — Random generation samples from $z \sim \mathcal{N}(0,I)$.
    \item \textbf{Figure: output\_cell\_25\_img\_0.png} — Latent interpolation grid between sample pairs.
    \item \textbf{Figure: output\_cell\_27\_img\_0.png} — 2D latent visualization (t-SNE) of smile/non-smile clusters.
    \item \textbf{Figure: output\_cell\_29\_img\_0.png} — Latent dimension utilization plot.
\end{itemize}

\begin{thebibliography}{00}
\bibitem{kingma2013auto} D. P. Kingma and M. Welling, ``Auto-encoding variational bayes,'' \textit{arXiv preprint arXiv:1312.6114}, 2013.

\bibitem{rezende2014stochastic} D. J. Rezende, S. Mohamed, and D. Wierstra, ``Stochastic backpropagation and approximate inference in deep generative models,'' in \textit{Proc. International Conference on Machine Learning}, 2014, pp. 1278--1286.

\bibitem{higgs2017beta} I. Higgins et al., ``$\beta$-VAE: Learning basic visual concepts with a constrained variational framework,'' in \textit{Proc. ICLR}, 2017.

\bibitem{larsen2016autoencoding} A. B. L. Larsen et al., ``Autoencoding beyond pixels using a learned similarity metric,'' in \textit{Proc. International Conference on Machine Learning}, 2016, pp. 1558--1566.

\bibitem{sohn2015learning} K. Sohn, H. Lee, and X. Yan, ``Learning structured output representation using deep conditional generative models,'' in \textit{Advances in Neural Information Processing Systems}, vol. 28, 2015.
\end{thebibliography}

\end{document}
