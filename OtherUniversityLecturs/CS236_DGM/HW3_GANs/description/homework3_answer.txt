                                CS 236 Homework 3 Solutions

                            Instructors: Stefano Ermon and Aditya Grover
                                  {ermon,adityag}@cs.stanford.edu

                         Available: 11/12/2018; Due: 23:59 PST, 12/3/2018


Problem 1: Generative adversarial networks (15 points)
   In this problem, we will implement a generative adversarial network (GAN) that models a high-dimensional
data distribution pdata (x), where x ∈ Rn . To do so, we will define a generator Gθ : Rk → Rn ; we obtain samples
from our model by first sampling a k-dimensional random vector z ∼ N (0, I) and then returning Gθ (z).
We will also define a discriminator Dϕ : Rn → (0, 1) that judges how realistic the generated images Gθ (z) are,
compared to samples from the data distribution x ∼ pdata (x). Because its output is intended to be interpreted
as a probability, the last layer of the discriminator is frequently the sigmoid function,
                                                             1
                                                 σ(x) =           ,
                                                          1 + e−x
which constrains its output to fall between 0 and 1. For convenience, let hϕ (x) denote the activation of the
discriminator right before the sigmoid layer, i.e. let Dϕ (x) = σ(hϕ (x)). The values hϕ (x) are also called the
discriminator’s logits.
There are several common variants of the loss functions used to train GANs. They can all be described as
a procedure where we alternately perform a gradient descent step on LD (ϕ; θ) with respect to ϕ to train the
discriminator Dϕ , and a gradient descent step on LG (θ; ϕ) with respect to θ to train the generator Gθ :

                                         min LD (ϕ; θ),       min LG (θ; ϕ).
                                          ϕ                    θ


In lecture, we talked about the following losses, where the discriminator’s loss is given by

                     LD (ϕ; θ) = −Ex∼pdata (x) [log Dϕ (x)] − Ez∼N (0,I) [log(1 − Dϕ (Gθ (z)))],

and the generator’s loss is given by the minimax loss

                                Lminimax
                                 G       (θ; ϕ) = Ez∼N (0,I) [log(1 − Dϕ (Gθ (z)))].

  1. [5 points] Unfortunately, this form of loss for LG suffers from a vanishing gradient problem. In terms of
     the discriminator’s logits, the minimax loss is

                                  Lminimax
                                   G       (θ; ϕ) = Ez∼N (0,I) [log(1 − σ(hϕ (Gθ (z))))].

     Show that the derivative of Lminimax
                                  G       with respect to θ is approximately 0 if D(Gθ (z)) ≈ 0, or equivalently,
     if hϕ (Gθ (z)) ≪ 0. You may use the fact that σ ′ (x) = σ(x)(1 − σ(x)). Why is this problematic for the
     training of the generator when the discriminator successfully identifies a fake sample Gθ (z)?




                                                          1
  Solution: Using the chain rule and the fact that σ ′ (x) = σ(x)(1 − σ(x)),

                    ∂Lminimax              [ σ ′ (h (G (z))) ∂                 ]
                                                   ϕ   θ
                      G
                              = Ez∼N (0,I)                         hϕ (Gθ (z))
                       ∂θ                    1 − σ(hϕ (Gθ (z))) ∂θ
                                           [ σ(h (G (z)))(1 − σ(h (G (z)))) ∂                   ]
                                                ϕ    θ              ϕ    θ
                              = Ez∼N (0,I)                                          hϕ (Gθ (z))
                                                     1 − σ(hϕ (Gθ (z)))          ∂θ
                                           [                ∂              ]
                              = Ez∼N (0,I) σ(hϕ (Gθ (z))) hϕ (Gθ (z))
                                                            ∂θ
                                           [             ∂             ]
                              = Ez∼N (0,I) Dϕ (Gθ (z)) hϕ (Gθ (z)) .
                                                         ∂θ
  When the discriminator successfully identifies a fake sample Gθ (z), it outputs Dϕ (Gθ (z)) ≈ 0, causing
   ∂Lminimax
     G
      ∂θ    ≈ 0. Because the update to the generator’s parameters is proportional to this gradient, the
  training of the generator slows or stops.
2. [10 points] Because of this vanishing gradient problem, in practice, Lminimax
                                                                         G       is typically replaced with
   the non-saturating loss

                                Lnon-saturating
                                 G              (θ; ϕ) = −Ez∼N (0,I) [log Dϕ (Gθ (z))].

  To turn the non-saturating loss into a concrete algorithm, we will take alternating gradient steps on Monte
  Carlo estimates of LD and Lnon-saturating
                               G            :

                                                1 ∑                    1 ∑
                                                   m                      m
                              LD (ϕ; θ) ≈ −           log Dϕ (x(i) ) −       log(1 − Dϕ (Gθ (z (i) )),
                                                m i=1                  m i=1
                                                1 ∑
                                                    m
                   Lnon-saturating
                    G              (θ; ϕ) ≈ −         log Dϕ (Gθ (z (i) )),
                                                m i=1

  where m is the batch size, and for i = 1, . . . , m, we sample x(i) ∼ pdata (x) and z (i) ∼ N (0, I).
  Implement and train a non-saturating GAN on Fashion MNIST for one epoch. Read through run_gan.py,
  and in codebase/gan.py, implement the loss_nonsaturating function. To train the model, execute
  python run_gan.py. You may monitor the GAN’s output in the out_nonsaturating directory. Note
  that because the GAN is only trained for one epoch, we cannot expect the model’s output to produce very
  realistic samples, but they should be roughly recognizable as clothing items.
  Please package all of the programming parts (1.2, 3.2, and 4.5) together using make_submission.sh and
  submit the resulting ZIP file on GradeScope.
  Solution:

  def loss_nonsaturating(g, d, x_real, *, device):
      batch_size = x_real.shape[0]
      z = torch.randn(batch_size, g.dim_z, device=device)
      x_fake = g(z)
      d_real = d(x_real)
      d_fake = d(x_fake)

       # Either of the below are acceptable
       d_loss = -F.logsigmoid(d_real).mean() - F.logsigmoid(-d_fake).mean()
       d_loss = F.binary_cross_entropy_with_logits(d_real,
                    torch.ones(batch_size, device=device)) + \
                F.binary_cross_entropy_with_logits(d_fake,
                    torch.zeros(batch_size, device=device))



                                                          2
          g_loss = -F.logsigmoid(d_fake).mean()
          return d_loss, g_loss


     (Note that 1 − σ(x) = σ(−x).)

Problem 2: Divergence minimization (25 points)
  Now, let us analyze some theoretical properties of GANs. For convenience, we will denote pθ (x) to be the
distribution whose samples are generated by first sampling z ∼ N (0, I) and then returning the sample Gθ (z).
With this notation, we may compactly express the discriminator’s loss as
                          LD (ϕ; θ) = −Ex∼pdata (x) [log Dϕ (x)] − Ex∼pθ (x) [log(1 − Dϕ (x))].

  1. [10 points] Show that LD is minimized when Dϕ = D∗ , where
                                                                 pdata (x)
                                                 D∗ (x) =                      .
                                                            pθ (x) + pdata (x)
     (Hint: for a fixed x, what t minimizes f (t) = −pdata (x) log t − pθ (x) log(1 − t)?)
     Solution: Following the hint, we set f ′ (t) = 0 to obtain that the optimal t satisfies
                                                          pdata (x) pθ (x)
                                                  0=−              +       ,
                                                              t      1−t
     or, solving for t,
                                                              pdata (x)
                                                    t=                      .
                                                         pθ (x) + pdata (x)
     This is the unique minimizer of f , since f is the sum of strictly convex functions.
     Then, we can write
                           LD (ϕ; θ) = −Ex∼pdata (x) [log Dϕ (x)] − Ex∼pθ (x) [log(1 − Dϕ (x))]
                                         ∫                            ∫
                                     = − pdata (x) log Dϕ (x) dx − pθ (x) log(1 − Dϕ (x)) dx
                                       ∫
                                     = f (Dϕ (x)) dx.

     To minimize LD , it suffices to minimize f (Dϕ (x)) for every x. We can do this by setting Dϕ (x) =
         pdata (x)
     pθ (x)+pdata (x) for every x.

  2. [5 points] Recall that Dϕ (x) = σ(hϕ (x)). Show that the logits hϕ (x) of the discriminator estimate the
     log of the likelihood ratio of x under the true distribution compared to the model’s distribution; that is,
     show that if Dϕ = D∗ , then
                                                             pdata (x)
                                                hϕ (x) = log           .
                                                               pθ (x)
     Solution: Note that
                                                                             1
                                             Dϕ (x) = σ(hϕ (x)) =                       .
                                                                        1 + e−hϕ (x)
     Setting this to the expression for D∗ (x) in part 1, we find that
                                                                 pθ (x) + pdata (x)
                                              1 + e−hϕ (x) =                        ,
                                                                      pdata (x)
     or
                                                       pθ (x) + pdata (x) − pdata (x)
                                          e−hϕ (x) =                                  .
                                                                 pdata (x)
     Invert and take the logarithm of both sides to arrive at the result.


                                                             3
  3. [5 points] Consider a generator loss defined by the sum of the minimax loss and the non-saturating loss,

                               LG (θ; ϕ) = Ex∼pθ (x) [log(1 − Dϕ (x))] − Ex∼pθ (x) [log Dϕ (x)].

      Show that if Dϕ = D∗ , then
                                              LG (θ; ϕ) = KL(pθ (x)||pdata (x)).
      Solution:

                                  LG (θ; ϕ) = Epθ (x) [log(1 − Dϕ (x))] − Epθ (x) [log Dϕ (x)]
                                                      [     1 − Dϕ (x)) ]
                                            = Epθ (x) log
                                                              Dϕ (x))
                                                      [      pθ (x)) ]
                                            = Epθ (x) log
                                                            pdata (x))
                                            = KL(pθ (x)||pdata (x)).

  4. [5 points] Recall that when training VAEs, we minimize the negative ELBO, an upper bound to the
     negative log likelihood. Show that the negative log likelihood, −Ex∼pdata (x) [log pθ (x)], can be written as
     a KL divergence plus an additional term that is constant with respect to θ. Does this mean that a VAE
     decoder trained with ELBO and a GAN generator trained with the LG defined in the previous part are
     implicitly learning the same objective? Explain.
      Solution: The VAE can be seen as minimizing an upper bound to
                                                                             [    pdata (x)) ]
          −Ex∼pdata (x) [log pθ (x)] + Ex∼pdata (x) [log pdata (x)] = Epθ (x) log              = KL(pdata (x)||pθ (x)).
                                                                                   pθ (x))

      However, in general, KL(pdata (x)||pθ (x)) ̸= KL(pθ (x)||pdata (x)), so the two objectives are not the same.

Problem 3: Conditional GAN with projection discriminator (20 points)
   So far, we have trained GANs that sample from a given dataset of images. However, many datasets come
with not only images, but also labels that specify the class of that particular image. In the MNIST dataset,
we have both the digit’s image as well as its numerical identity. It is natural to want to generate images that
correspond to a particular class.
Formally, an unconditional GAN is trained to produce samples x ∼ pθ (x) that mimic samples x ∼ pdata (x)
from a data distribution. In the class-conditional setting, we instead have have labeled data (x, y) ∼ pdata (x, y)
and seek to train a model pθ (x, y). Since it is the class conditional generator pθ (x|y) that we are interested
in, we will express pθ (x, y) = pθ (x|y)pθ (y). We will set pθ (x|y) to be the distribution given by Gθ (z, y), where
z ∼ N (0, I) as usual. For simplicity, we will assume pdata (y) = m   1                   1
                                                                         and set pθ (y) = m , where m is the number
of classes. In this case, the discriminator’s loss becomes

          LD (ϕ; θ) = −E(x,y)∼pdata (x,y) [log Dϕ (x, y)] − E(x,y)∼pθ (x,y) [log(1 − Dϕ (x, y))]
                    = −E(x,y)∼pdata (x,y) [log Dϕ (x, y)] − Ey∼pθ (y) [Ez∼N (0,I) [log(1 − Dϕ (Gθ (z, y), y))]].

Therefore, the main difference for the conditional GAN is that we must structure our generator Gθ (z, y) and
discriminator Dϕ (x, y) to accept the class label y as well. For the generator, one simple way to do so is to encode
y as a one-hot vector y and concatenate it to z, and then apply neural network layers normally. (A one-hot
representation of a class label y is an m-dimensional vector y that is 1 in the yth entry and 0 everywhere else.)
In practice, the effectiveness of the model is strongly dependent on the way the discriminator depends on y. One
heuristic with which to design the discriminator is to mimic the form of the theoretically optimal discriminator.
That is, we can structure the neural network used to model Dϕ based on the form of D∗ , where D∗ minimizes
LD . To calculate the theoretically optimal discriminator, though, it is necessary to make some assumptions.

  1. [10 points] Suppose that when (x, y) ∼ pdata (x, y), there exists a feature mapping φ under which φ(x)
     becomes a mixture of m unit Gaussians, with one Gaussian per class label y. Assume that when (x, y) ∼


                                                            4
  pθ (x, y), φ(x) also becomes a mixture of m unit Gaussians, again with one Gaussian per class label y.
  Concretely, we assume that the ratio of the conditional probabilities can be written as
                                        pdata (x|y)   N (φ(x)|µy , I)
                                                    =                  ,
                                         pθ (x|y)     N (φ(x)|µ̂y , I)
  where µy and µ̂y are the means of the Gaussians for pdata and pθ respectively.
  Show that under this simplifying assumption, the optimal discriminator’s logits h∗ (x, y) can be written
  in the form
                                        h∗ (x, y) = y T (Aφ(x) + b)
  for some matrix A and vector b, where y is a one-hot vector denoting the class y. In this problem,
  the discriminator’s output and logits are related by Dϕ (x, y) = σ(hϕ (x, y)). (Hint: use the result from
  problem 2.2.)
  Solution:
                               pdata (x, y)
               hϕ (x, y) = log
                                pθ (x, y)
                               pdata (x|y)         pdata (y)
                        = log                + log
                                pθ (x|y)            pθ (y)
                               pdata (x|y)
                        = log
                                pθ (x|y)
                               exp(− 12 ||φ(x) − µy ||2 )
                        = log
                               exp(− 12 ||φ(x) − µ̂y ||2 )
                             1                     1
                        = − ||φ(x) − µy ||2 + ||φ(x) − µ̂y ||2
                             2                     2
                             1                            1            1                      1
                        = − ||φ(x)|| − µy φ(x) − ||µy ||2 + ||φ(x)||2 + µ̂Ty φ(x) + ||µ̂y ||2
                                       2      T
                             2                            2            2                      2
                                                  1
                        = (µ̂y − µy )T φ(x) + (||µ̂y ||2 − ||µy ||2 )
                                                  2
                                                                                       
                                   (µ̂1 − µ1 )T                 1
                                                                  (||µ̂1 ||2 − ||µ1 ||2 )
                                                            2                          
                                        ..                               ..            
                        = y T            .       φ(x) +                   .            
                                                                                       
                                  (µ̂m − µm )T                 1
                                                               2 (||µ̂m  ||2
                                                                              − ||µm ||2
                                                                                         )

2. [10 points] Implement and train a conditional GAN on Fashion MNIST for one epoch. The discriminator
   has the structure described in part 1, with φ, A and b parameterized by a neural network with a final
   linear layer, and the generator accepts a one-hot encoding of the class. In codebase/gan.py, implement the
   conditional_loss_nonsaturating function. To train the model, execute python run_conditional_gan.py.
   You may monitor the GAN’s output in the out_nonsaturating_conditional directory. You should be
   able to roughly recognize the categories that correspond to each column.
  Solution:

  def conditional_loss_nonsaturating(g, d, x_real, *, device):
      batch_size = x_real.shape[0]
      z = torch.randn(batch_size, g.dim_z, device=device)
      x_fake = g(z, y_real)
      d_real = d(x_real, y_real)
      d_fake = d(x_fake, y_real)

       d_loss = -F.logsigmoid(d_real).mean() - F.logsigmoid(-d_fake).mean()
       g_loss = -F.logsigmoid(d_fake).mean()
       return d_loss, g_loss



                                                    5
Problem 4: Wasserstein GAN (35 points)
  In many cases, the GAN algorithm can be thought of as minimizing a divergence between a data distribution
pdata (x) and the model distribution pθ (x). For example, the minimax GAN discussed in the lectures minimizes
the Jensen-Shannon divergence, and the loss in problem 2.3 minimizes the KL divergence. In this problem, we
will explore an issue with these divergences and one potential way to fix it.

  1. [5 points] Let pθ (x) = N (x|θ, ϵ2 ) and pdata (x) = N (x|θ0 , ϵ2 ) be normal distributions with standard
     deviation ϵ centered at θ ∈ R and θ0 ∈ R respectively. Show that

                                                                        (θ − θ0 )2
                                            KL(pθ (x)||pdata (x)) =                .
                                                                           2ϵ2

     Solution:
                                                                [      exp(− 2ϵ12 (x − θ)2 ) ]
                           KL(pθ (x)||pdata (x)) = Ex∼N (θ,ϵ2 ) log
                                                                      exp(− 2ϵ12 (x − θ0 )2 )
                                                                [ 1 (                               )]
                                                 = Ex∼N (θ,ϵ2 )        −(x −  θ) 2
                                                                                    + (x  − θ 0 ) 2
                                                                  2ϵ2
                                                                [ 1                                 ]
                                                 = Ex∼N (θ,ϵ2 )       (2xθ − 2xθ   0 − θ 2
                                                                                           +  θ 2
                                                                                                0 )
                                                                  2ϵ2
                                                    1
                                                 = 2 (2θ2 − 2θθ0 − θ2 + θ02 )
                                                   2ϵ
                                                   (θ − θ0 )2
                                                 =            .
                                                      2ϵ2

  2. [5 points] Suppose pθ (x) and pdata (x) both place probability mass in only a very small part of the domain;
     that is, consider the limit ϵ → 0. What happens to KL(pθ (x)||pdata (x)) and its derivative with respect to
     θ, assuming that θ ̸= θ0 ? Why is this problematic for a GAN trained with the loss function LG defined in
     problem 2.3?
     Solution: Unless θ = θ0 , both KL(pθ (x)||pdata (x)) and its derivative go to infinity. If the discriminator
     is trained to optimality, the generator will receive extremely large gradients and hence training will be
     unstable.
  3. [5 points] To avoid this problem, we’ll propose an alternative objective for the discriminator and generator.
     Consider the following alternative objectives:

                                   LD (ϕ; θ) = Ex∼pθ (x) [Dϕ (x)] − Ex∼pdata (x) [Dϕ (x)]
                                   LG (θ; ϕ) = −Ex∼pθ (x) [Dϕ (x)],

     where Dϕ is no longer constrained to functions that output a probability; instead Dϕ can be a function
     that outputs any real number. As defined, however, these losses are still problematic. Again consider the
     limit ϵ → 0; that is, let pθ (x) be the distribution that outputs θ ∈ R with probability 1, and let pdata (x) be
     the distribution that outputs θ0 ∈ R with probability 1. Why is there no discriminator Dϕ that minimizes
     this new objective LD ?
     Solution: Unless θ = θ0 , LD can approach −∞ by setting Dϕ (θ) → −∞ or Dϕ (θ0 ) → ∞. Therefore
     there is no discriminator that minimizes LD .
  4. [5 points] Let’s tweak the alternate objective so that an optimal discriminator exists. Consider the same
     objective LD and the same limit ϵ → 0. Now, suppose that Dϕ is restricted to differentiable functions
     whose derivative is always between −1 and 1. It can still output any real number. Is there now a
     discriminator Dϕ out of this class of functions that minimizes LD ? Briefly describe what the optimal Dϕ
     looks like as a function of x.
     Solution: Let c = Dϕ (θ0 ). Because Dϕ has slope between −1 and 1, the smallest Dϕ (θ) can be is
     c − |θ − θ0 |, which is achieved by setting Dϕ to be a straight line of slope ±1 connecting the points (θ0 , c)
     and (θ, c − |θ − θ0 |).


                                                            6
5. [15 points] The Wasserstein GAN with gradient penalty (WGAN-GP) enables stable training by penal-
   izing functions whose derivatives are too large. It achieves this by adding a penalty on the 2-norm of the
   gradient of the discriminator at various points in the domain. It is defined by

              LD (ϕ; θ) = Ex∼pθ (x) [Dϕ (x)] − Ex∼pdata (x) [Dϕ (x)] + λEx∼rθ (x) [(||∇Dϕ (x)||2 − 1)2 ]
              LG (θ; ϕ) = −Ex∼pθ (x) [Dϕ (x)],

  where rθ (x) is defined by sampling α ∼ Uniform([0, 1]), x1 ∼ pθ (x), and x2 ∼ pdata (x), and returning
  αx1 + (1 − α)x2 . The hyperparameter λ controls the strength of the penalty; a setting that usually works
  is λ = 10.
  Implement and train WGAN-GP for one epoch on Fashion MNIST. In codebase/gan.py, implement
  the loss_wasserstein_gp function. To train the model, execute python run_gan.py --loss_type
  wasserstein_gp. You may monitor the GAN’s output in the out_wasserstein_gp directory.
  Solution:

  def loss_wasserstein_gp(g, d, x_real, *, device):
      batch_size = x_real.shape[0]
      z = torch.randn(batch_size, g.dim_z, device=device)
      x_fake = g(z)
      d_real = d(x_real)
      d_fake = d(x_fake)

       alpha = torch.rand(x_real.shape[0], 1, 1, 1, device=device)
       x_r = alpha * x_fake + (1 - alpha) * x_real
       d_r = d(x_r)

       grad = torch.autograd.grad(d_r.sum(), x_r, create_graph=True)
       grad_norm = grad[0].reshape(batch_size, -1).norm(dim=1)
       d_loss = (d_fake - d_real).mean() + 10 * ((grad_norm - 1)**2).mean()
       g_loss = -d_fake.mean()
       return d_loss, g_loss


  Note the use of d_r.sum(). We are trying to obtain the following matrix of derivatives (we will then take
  the norm of each row, for use in the gradient penalty):
                                                         (1)
                                                              
                                                           ∂D(x )
                                                           ∂x(1) 
                                                              ..      
                                               grad =          .      .
                                                                      
                                                           ∂D(x(m) )
                                                            ∂x(m)

                                         (i)
  In principle, we could compute ∂D(x
                                    ∂x(i)
                                          )
                                            using a for loop over each element in the batch and then stack
  the resulting derivatives. However, this is inefficient. Instead, notice that

                                                       ∂ ∑
                                                             m
                                          ∂D(x(i) )
                                                    =           D(x(j) ),
                                           ∂x(i)      ∂x(i) j=1

  because each D(x(j) ) is constant w.r.t. x(i) for i ̸= j. Therefore, if we let
                                                              
                                                          x(1)
                                                              
                                                         . 
                                                  X =  ..  ,
                                                              
                                                          x(m)


                                                       7
      we find that
                                                            ∂ ∑
                                                                 m
                                                  grad =          D(x(j) ).
                                                           ∂X j=1


Problem 5: Noise contrastive estimation (5 points)
  Noise contrastive estimation is a technique to learn an energy-based model pθ (x) = Z1 exp(−E∫   θ (x)) to model
a data distribution pdata (x). (Recall that pθ is only a proper probability distribution if Z = exp(−Eθ (x)) dx.)
Noise contrastive estimation (NCE) requires access to a second, tractable probabilistic model pnoise (x) and
optimizes the following objective:

max Ex∼pdata (x) [log σ(−Eθ (x) − log Z − log pnoise (x))] + Ex∼pnoise (x) [log(1 − σ(−Eθ (x) − log Z − log pnoise (x)))].
θ,Z


   1. [5 points] Let (θ∗ , Z ∗ ) be the parameters that maximize this objective. Show that Z1∗ exp(−Eθ∗ (x)) =
      pdata (x). (Hint: use the result from problem 2.2.)
      Solution: Using problem 2.2,

                                                                                 pdata (x)
                                     −Eθ∗ (x) − log Z ∗ − log pnoise (x) = log              .
                                                                                 pnoise (x)

      The results follows from exponentiating both sides.




                                                            8
